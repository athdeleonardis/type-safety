{
  "name": "type-safety",
  "version": "1.0.0",
  "description": "A library for type safety (validation and reformatting) in JavaScript, written in TypeScript.",
  "scripts": {
    "build": "npx tsc",
    "test": "npm run build && node dist/test/schema/test-type-string.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.8.7",
    "typescript": "^5.6.3"
  }
}
